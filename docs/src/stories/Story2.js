import React from 'react'

export default props => (
  <div>
    Hey, look! Another react-story.
    <br />
    <pre><code>{JSON.stringify(props, null, 2)}</code></pre>
  </div>
)

export const knobs = [

]
